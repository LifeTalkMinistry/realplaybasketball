(() => {
  if (window.__realPlayCourtsideLiveInstalled) return;
  window.__realPlayCourtsideLiveInstalled = true;

  // RETIRED: FUTURE/LIVE courtside scoring is no longer part of Real Play.
  // Official scoring now uses the recorded-video workflow only.
})();
